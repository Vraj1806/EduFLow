# Project-level npm config — the user's global config sets omit=dev, which
# would skip devDependencies (vitest, tsx, prisma, @types, etc.). Re-include
# them so installs at the workspace root are complete for development.
include=dev
